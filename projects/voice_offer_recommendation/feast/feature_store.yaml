project: voice_offer_recommendation
registry: data/registry.db
provider: local
online_store:
    type: postgres
    host: ${SUPABASE_DB_HOST}
    port: 5432
    database: postgres
    db_schema: feast
    user: ${SUPABASE_DB_USER}
    password: ${SUPABASE_DB_PASSWORD}
entity_key_serialization_version: 2
