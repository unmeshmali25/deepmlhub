# ServiceAccount for inference workloads
apiVersion: v1
kind: ServiceAccount
metadata:
  name: inference-sa
  namespace: ml-inference
  annotations:
    # This will be updated with the actual GCP service account
    # iam.gke.io/gcp-service-account: inference-sa@deepmlhub-voiceoffers.iam.gserviceaccount.com
